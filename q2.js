function myFunction(num1, num2) {
    num1+num2
  }
  
  const result = myFunction(5,5)

  // expected result = function will run but there is nothing to return 

  console.log(result)

  // the reason it is undifined is beacuse we log result but there is nothing in the function so we get undifined 