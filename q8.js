function myFunction(num1, num2) {
    return num1 + num2
  }
  
  const num1 = 10
  const num2 = 1
  const num3 = 4
  
  const result = myFunction(num3, num1)

  // the result will be 14 because num1 and num3 are the arguments 

  console.log(result)